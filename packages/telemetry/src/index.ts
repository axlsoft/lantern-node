// Core Lantern telemetry SDK — implementation begins in Phase 1.5
export interface LanternClientOptions {
  endpoint: string;
  apiKey: string;
}

export class LanternClient {
  constructor(_options: LanternClientOptions) {}
}
